import React from "react";

import { ChatItemView } from "../ChatItenView";
import { ListItem } from "@material-ui/core";

describe("render ChatIten", () => {
  it("returns ListItem", () => {
      const expected = render(<ListItem><Link><span>Hello</span></Link></ListItem>);
      const receved =
  });
});
